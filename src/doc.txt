Note: javac -cp "lib/*" src/*.java  
This is important for loading all library again 
java -cp "lib/*:src" Server   ,, then run 

Delete all classes - find src -name "*.class" -delete 
Recompile server - javac -cp "lib/*" $(find src -name "*.java")

//compile and run a single class main method ..
javac src/auth/PasswordUtil.java
java -cp src auth.PasswordUtil

// admin pass - imtheadminbitch
// user pass - testuser123